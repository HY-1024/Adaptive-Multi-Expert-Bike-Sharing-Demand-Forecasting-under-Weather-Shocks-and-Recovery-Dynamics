# Confirmation experiment input

`expert_predictions.csv` contains the frozen issue-time predictions required by the confirmation experiments. It keeps only split/state identifiers, four targets and target timestamps, and the raw predictions of E1-E6. It was extracted from the completed six-expert run before historical run directories were archived.

The table deliberately excludes target-time weather and future event information.
